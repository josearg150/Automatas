#include <iostream>
#include <string>
#include <cstring>
using namespace std;

int main()
{
    /*Mi propuesta
    string nombre ;
    cout << "Ingresa el nombre: ";
    getline(cin, nombre);

    int n  = nombre.length();
    char arregloCons[n+1];

    strcpy(arregloCons, nombre.c_str());
    int cont = 0;
    for (int i = 0; i < n; i++) {
        if(arregloCons[i] == 'a' ||
           arregloCons[i] == 'e' ||
           arregloCons[i] == 'i' ||
           arregloCons[i] == 'o' ||
           arregloCons[i] == 'u' )
           cont ++;
    }

    char consonantes[n-cont];
    for (int i = 0; i < n; i++) {
        if(arregloCons[i] != 'a' &&
           arregloCons[i] != 'e' &&
           arregloCons[i] != 'i' &&
           arregloCons[i] != 'o' &&
           arregloCons[i] != 'u' )
           consonantes[i] = arregloCons[i];
    }
     for (int i = 0; i < n; i++) {
         cout << consonantes[i];
     }
    cout <<""<<endl;*/
}
